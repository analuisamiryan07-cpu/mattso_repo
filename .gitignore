# Dependencies
node_modules/

# Build
dist/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Migration leftovers (do not commit)
raw_pages/
Campus Matsso*_files/
*.ps1
extract.js
fix_css.js
fix_html.js
first_lines.txt
header*.txt
search_header.txt
test.txt

# Logs
*.log
npm-debug.log*

# Backend
backend-matsso/dist/
backend-matsso/node_modules/
backend-matsso/.env
backend-matsso/.env.local
backend-matsso/.env.*.local
!backend-matsso/.env.example

# PHP Vendor
proyecto_matt/vendor/

# PHP env files
proyecto_matt/.env
proyecto_matt/.env.local

# Credenciales de sincronización (van en /etc/matsso/ en el servidor)
scripts/.env.sync

# Archivos generados por el módulo de documentos (no deben ir a Git)
proyecto_matt/generados/
proyecto_matt/test_clean.txt
proyecto_matt/test_xml.txt

# Comprobantes de pago subidos al backend
backend-matsso/uploads/

# Scan reports SAST (archivos de auditoría locales)
*.pdf
scan*.manifest
scan*.json
SASTAgent_*.log
analyzer_*.log
report_text.txt

# Scripts del servidor (configuración interna, referencias a credenciales)
scripts/

# Servicios independientes (no se despliegan en cloud)
certibot/

# Paquetes de entrega y documentación interna
entrega_servidor_2026-07-15/
A Detalle/
ENTREGA_MATSSO_SERVIDOR_2026-07-15.zip.sha256

# Scripts SQL del servidor (esquema completo de BD — información sensible)
db_scripts/

# Comprobantes subidos en tiempo de ejecución (ya excluidos arriba)
# backend-matsso/src/storage/ ← NO excluir: es código fuente del módulo

# Backoffice interno — corre en servidor local (192.168.100.18), no en la nube
proyecto_matt/

